import { BooleanfilterPipe } from './booleanfilter.pipe';

describe('BooleanfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
